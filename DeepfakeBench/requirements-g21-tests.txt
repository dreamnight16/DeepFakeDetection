# Install in the existing SERVER environment. Do not replace its CUDA torch build.
pytest>=8,<9
pytest-cov>=5,<7
PyYAML>=6,<7
# Runtime: existing torch (>=2.2), numpy, OpenCV, transformers, loralib and
# original DeepfakeBench dependencies. The tests never download/load real CLIP.
